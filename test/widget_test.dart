import 'package:flutter_test/flutter_test.dart';

void main() {
  // Smoke tests are skipped until platform dependencies (SQLite, notifications)
  // are mocked. Add integration tests in test/integration/ instead.
  test('placeholder', () => expect(true, isTrue));
}
